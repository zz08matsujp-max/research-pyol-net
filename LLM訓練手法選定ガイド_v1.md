# LLM 訓練手法選定ガイド v1

**作成日**: 2026-05-13
**対象読者**: エンジニア視点で LLM 訓練手法の全体像を理解したい個人開発者
**目的**: 「LLM 訓練」と一言で言われるものの中に、実は 6 段階の階層があることを明らかにし、自分のプロジェクトに適切な階層を選定できるようにする

---

## 1. このガイドの基本認識

「LLM を訓練する」という言葉は、実際には次の 6 つの異なる作業を指している:

1. プロンプトの工夫
2. 少数例の埋め込み
3. 外部知識との連携
4. 軽量な重み調整
5. 全パラメータ調整
6. ゼロから事前学習

**最大の落とし穴**: 多くの初心者は「LLM 訓練 = #6 ゼロから作る」と誤解する。実際には個人開発者にとっての現実的な選択肢は **#1〜#4** のみであり、#5 以上は企業案件である。

エンジニアとしては、まず **「自分が本当に必要なのは何階層目か」** を見極めることが、リソースを無駄にしない最初の判断となる。

---

## 2. 全体像：6 段階の階層

### 比較表

| # | 手法 | 重みは変わる？ | データ量 | 計算資源 | 期間 | 概算費用（個人） |
|---|---|---|---|---|---|---|
| 1 | Prompt Engineering | ❌ | 0 | API のみ | 数時間〜数日 | 数百〜数千円 |
| 2 | Few-shot Learning | ❌（実行時のみ） | 数〜数十件 | API のみ | 数日 | 数千〜数万円 |
| 3 | RAG | ❌ | 数百〜数千文書 | 軽量サーバー | 1〜4週間 | 月数千〜数万円 |
| 4 | LoRA / QLoRA | ✅（一部） | 1,000〜10,000例 | GPU 1〜2枚 | 1〜3ヶ月 | 一回 数万〜十数万円 |
| 5 | Full Fine-tuning | ✅（全部） | 1万〜10万例 | GPU 4〜8枚 | 半年 | 数十万〜数百万円 |
| 6 | Pretraining | ✅（ゼロから） | 数十億トークン | GPU 数百〜数千枚 | 1年以上 | 数億〜数百億円 |

### エンジニア視点の類推

```
#1 Prompt Engineering   ≒ コンフィグファイル調整
#2 Few-shot Learning    ≒ 関数呼び出し時に例を渡す
#3 RAG                  ≒ DB ルックアップ + ロジック
#4 LoRA Fine-tuning     ≒ オーバーレイパッチ
#5 Full Fine-tuning     ≒ 全コードのリビルド
#6 Pretraining          ≒ CPU 設計から始める
```

---

## 3. 各手法の詳細

### 3.1 Prompt Engineering（プロンプト工学）

**何をするか**: LLM に投げる指示文（プロンプト）の構造・表現・出力フォーマット指定を磨く。モデル本体は何も変えない。

**仕組み**:
```
[システム] あなたは詐欺検出の専門家です。次の文章を 108 煩悩のうちどれを刺激しているか判定し、JSON で返答せよ。
[煩悩リスト] 貪・瞋・癡・…
[出力フォーマット] {"primary_bonno": "貪", "score": 0.85, "reason": "..."}
[ユーザー入力] お時間がありません。今すぐ振込を…
```

**メリット**:
- 即時実装可能
- API 利用料のみ
- いつでも調整可能
- バージョン管理しやすい

**デメリット**:
- プロンプトが長くなるほど API 料金が上昇
- 複雑な分類は限界がある
- モデルの「気分」（実行時のばらつき）に影響される

**個人開発でのポジション**: **必ず最初に通る関門**。これを煮詰めずに上の階層に行くのは、コンフィグを見直さずにコードを書き直すようなもの。

---

### 3.2 Few-shot Learning（少数例学習）

**何をするか**: プロンプトの中に「正解例」を 3〜10 個埋め込む。LLM はその場で例から学習して判定する。**ただし重みは一切変わらない** ── 一回ごとの「その場限りの学習」である。

**仕組み**:
```
[例1] 文「もう時間がない」 → 判定: 「焦燥煩悩を刺激」
[例2] 文「特別なあなただけに」 → 判定: 「自尊煩悩を刺激」
[例3] 文「今を逃したら一生損」 → 判定: 「貪欲煩悩を刺激」
[本番] 文「あなたなら特別に教えます」 → 判定: ?
```

**メリット**:
- Prompt Engineering の延長で簡単
- 例を入れ替えるだけで対応範囲を変えられる
- 学習データが少なくても効く

**デメリット**:
- 例の長さの分だけ API 料金が増える
- 例の選び方が結果を支配する
- 矛盾する例を入れると性能が落ちる

**個人開発でのポジション**: Prompt Engineering の自然な拡張。**Phase 1 の到達点**としては十分な力を持つ。

---

### 3.3 RAG（Retrieval-Augmented Generation）

**何をするか**: 外部の知識ベース（書籍、論文、過去事例など）をベクトル化して保存し、ユーザー入力が来たら **関連する知識を検索** して LLM に渡す。

**仕組み**:
```
[準備フェーズ]
仏教文献・心所論・詐欺事例
    ↓ Embedding API（文章を 1,500次元のベクトルに変換）
ベクトル DB（Pinecone / Qdrant / pgvector など）

[実行フェーズ]
ユーザー入力
    ↓ Embedding
ベクトル検索（類似度トップ 3〜5）
    ↓
関連文書 + ユーザー入力 → LLM → 判定結果
```

**メリット**:
- LLM の重みを触らずに、知識を更新できる
- 出典が明示できる（学術的に重要）
- 低コストで運用できる
- 知識ベースが大きくなっても LLM のサイズは変わらない

**デメリット**:
- 検索精度が結果を直接左右する
- LLM 自体の判断力は向上しない
- 適切な「チャンク分割」（文書をどう切るか）が技術的に難しい

**個人開発でのポジション**: **コスパが最強の階層**。集めた資料がそのまま知識源になる。本構想（PYOL マインドミラー）にとって、最も投資対効果が高い領域。

---

### 3.4 LoRA / QLoRA Fine-tuning（軽量微調整）

**何をするか**: モデルの一部の重みだけを、小さな追加パラメータ（数MB〜数百MB）で書き換える。元のモデルは凍結したまま、上に **薄い適応層** をかぶせる。

**仕組み**:
```
元モデル W （数十 GB、凍結）
       ＋
LoRA 重み ΔW = A × B （数百 MB、これだけ訓練）
       ＝
タスク特化モデル W + ΔW
```

**LoRA**: Low-Rank Adaptation。「重み変化は低ランク行列で近似できる」という仮定に基づく。

**QLoRA**: LoRA + Quantization（量子化）。さらにメモリ使用量を削減し、コンシューマー GPU でも実行可能にした手法。

**メリット**:
- 個人向け GPU（RTX 4090 など）でも実行できる
- 複数の LoRA を切り替えて使える
- 元モデルが温存されるので、複数のタスクに使い回せる

**デメリット**:
- **ラベル付きデータが必要**（最大の負荷）
- データ品質が結果を直接支配する
- 評価方法の設計が難しい
- 過学習（Overfitting）に注意

**必要なデータ**:
- 「文章 → 煩悩分類」のペアが **1,000〜10,000 件**
- 質より量より、**ラベルの一貫性** が決定的

**個人開発でのポジション**: 本格的な研究フェーズ。**論文化対象としては最も適切な階層**。

---

### 3.5 Full Fine-tuning（全パラメータ微調整）

**何をするか**: モデル全体の重みを書き換える。

**個人開発者には基本的に不要**。理由:
- LoRA で頭打ちになるまで、Full は意味が薄い
- コストが LoRA の 10〜100 倍
- データ要求も 10 倍以上

**例外**: 業界特化型の独自モデルを商用で売る企業案件、または学術的に Full のパフォーマンス上限を測りたい研究案件。

---

### 3.6 Pretraining（事前学習）

**何をするか**: ゼロから LLM を学習させる。

**Anthropic / OpenAI / Google / Meta / 東工大-産総研（Swallow）** レベルの事業。個人で挑むことは、現代の Linux カーネルを一人で書き直すようなもので、技術的にも経済的にも非現実的。

**例外**: ない。**個人プロジェクトでこの階層を選択肢に入れてはいけない**。

---

## 4. 選定フローチャート

```
START
  │
  ▼
タスクは既存 LLM で「ほぼ」できているか？
  │
  ├─ YES → #1 Prompt Engineering を煮詰める
  │
  ▼ NO
特定ドメインの知識が必要か？
  │
  ├─ YES → #3 RAG で外部知識を持たせる
  │
  ▼ NO
判定パターン自体を学ばせたいか？
  │
  ├─ YES → ラベル付きデータが 1,000+ 件あるか？
  │         │
  │         ├─ YES → #4 LoRA Fine-tuning
  │         │
  │         └─ NO  → まずデータ準備から（半年仕事）
  │
  ▼ NO
立ち止まって、本当に LLM 訓練が必要か再考する
```

**鉄則**: **下の階層で済むことは、下の階層で済ませる**。エンジニアリングと同じで、不必要な複雑化は悪である。

---

## 5. データの重要性

LLM 訓練で最も誤解されている事実:

> **「モデル構造より、データの方が 9 割の重要度を持つ」**

これは経験的事実であり、多くの研究で繰り返し確認されている。具体的には:

| 要素 | 重要度（相対） |
|---|---|
| データの量と質 | 70% |
| 評価方法の設計 | 15% |
| ハイパーパラメータ調整 | 10% |
| モデルアーキテクチャ選択 | 5% |

つまり「どのモデルを使うか」よりも、「何をどれだけ食わせるか」「どう測るか」の方が支配的である。これは CineBASIC 開発で経験された「生成 AI の揺らぎ」の本質でもある。揺らぎはモデルの問題ではなく、訓練データと評価の問題である。

### データ品質の 5 つの軸

1. **正確性**: ラベルが本当に正しいか
2. **一貫性**: 同じ入力に同じラベルが付いているか（複数の人がラベル付けすると必ず揺れる）
3. **多様性**: 偏ったデータでないか（例: 同じ詐欺パターンばかり）
4. **代表性**: 実運用で遭遇する分布を反映しているか
5. **クリーンさ**: 重複・ノイズ・誤字が除去されているか

### データ収集・ラベル付けの現実

- ラベル付けは **「コードを書く時間より長い」** ことが普通
- 1,000 件のラベル付けには 20〜100 時間かかる
- 複数人でラベル付けすると、必ず食い違いが出る（カッパ係数で測る）
- 「曖昧な例」のラベル付けに最も時間がかかる

---

## 6. 評価方法の設計

訓練前に必ず決めておくべきこと:

### 6.1 評価セットの分離

```
全データ
  │
  ├─ 訓練用（70%）── モデルを学習させる
  ├─ 検証用（15%）── ハイパーパラメータ調整
  └─ テスト用（15%）── 最終評価（一切学習に使わない）
```

**鉄則**: テスト用データを訓練に使ってしまった瞬間、評価は意味を失う。

### 6.2 評価指標

詐欺検出のような分類タスクでは:

| 指標 | 意味 | 重視するケース |
|---|---|---|
| Accuracy（正解率） | 全体的な正答率 | データが偏っていない時 |
| Precision（適合率） | 「詐欺」と判定したうち本当に詐欺の割合 | 偽陽性を避けたい時 |
| Recall（再現率） | 本当の詐欺のうち検出できた割合 | 見逃しを避けたい時 |
| F1 Score | Precision と Recall の調和平均 | バランス重視 |

**詐欺検出では Recall（見逃し回避）を優先することが多い**。ただし偽陽性が多すぎるとユーザーが警告慣れする問題があるため、PR バランスが重要。

### 6.3 人間評価の併用

数値指標だけでなく、専門家による定性評価も必須:
- 仏教学者: 煩悩分類が学問的に妥当か
- 心理学者: 心理メカニズム解釈が正しいか
- 一般ユーザー: 説明が理解可能か

---

## 7. よくある落とし穴

### 7.1 「すごいモデル」幻想

最新の SOTA モデルを使えば良くなる、という思い込み。実際には:
- データが悪ければ、最新モデルでも悪い結果しか出ない
- 古いモデル + 良いデータ > 新しいモデル + 悪いデータ

### 7.2 評価なき訓練

「とりあえず訓練してみる」は最悪。評価方法が固まっていないと、改善方向が分からない。

### 7.3 過学習（Overfitting）

訓練データには完璧に答えられるが、新しいデータには使えない状態。
- 対策: 検証セットでの性能を継続監視
- 対策: 訓練データを増やす、Regularization を強める

### 7.4 データリーク

訓練データに、テストデータと同じ内容が紛れ込む現象。評価が水増しされる。
- 対策: 厳密にデータを分離する
- 対策: 時系列分離（古いデータで訓練、新しいデータで評価）

### 7.5 ラベル付けのバイアス

一人でラベル付けすると、その人の偏りがそのままモデルに移る。
- 対策: 複数人でラベル付け、合意したものだけ採用
- 対策: 曖昧な例は別カテゴリに分ける

### 7.6 「無料 GPU」の罠

Google Colab 無料枠で数時間訓練して落ちる。
- 対策: 最初から Colab Pro+ または RunPod を使う
- 対策: 小さなデータでドライランしてから本番訓練

---

## 8. 個人開発者向け：実用的な選択肢

### コンピューティング環境

| 環境 | コスト | 適性 |
|---|---|---|
| OpenAI API | 従量課金 | #1〜#3 |
| Anthropic API | 従量課金 | #1〜#3 |
| Google Colab（無料）| 0円 | 試行のみ |
| Google Colab Pro+ | 月 5,000円程度 | #4 小規模 |
| RunPod / Vast.ai | 時間 100円〜 | #4 本格 |
| Lambda Labs | 時間 数百円〜 | #4 本格 |
| 自宅 RTX 4090 | 初期 30万円 | #4 ヘビーユーザー向け |

### モデル選択

日本語タスクなら:
- **Swallow LLM**（東工大+産総研）── 日本語特化、商用可能
- **Llama 3 8B** ── 多言語、軽量
- **Mistral 7B** ── 多言語、軽量
- **GPT-4o / Claude 3.5 / Gemini** ── API 経由（訓練不可）

API モデル（GPT-4o, Claude）は「触れない」かわりに圧倒的に高性能。
ローカルモデル（Swallow, Llama）は「触れる」かわりに性能はやや劣る。

---

## 9. 用語集

| 用語 | 意味 |
|---|---|
| トークン | LLM が扱う最小単位。日本語では 1文字 ≒ 0.5〜2トークン |
| Embedding | 文章を数値ベクトル（通常 384〜1,536次元）に変換すること |
| Fine-tuning | 既存モデルを特定タスクに合わせて追加訓練すること |
| Pretraining | ゼロからモデルを訓練すること |
| Inference | 訓練済みモデルを使って予測すること |
| Quantization | モデルの重みを低精度で表現してメモリを削減する技術（16bit → 4bit など）|
| Vector DB | Embedding を保存し、類似度検索できるデータベース |
| Hyperparameter | 学習率、バッチサイズなど、人間が設定する訓練パラメータ |
| Epoch | 全訓練データを 1 周すること |
| Loss | モデルの誤差。これを最小化するように訓練する |
| Gradient | Loss を減らす方向の勾配。誤差逆伝播で計算 |
| Attention | LLM 内部で「どこに注目するか」を計算する機構 |
| Context Window | LLM が一度に扱える最大トークン数（GPT-4o は 128K） |

---

## 10. 結論

LLM 訓練は **6 段階の階層** からなり、個人開発者にとっての実用的範囲は **#1〜#4** までである。下の階層で済むことは下の階層で済ませ、必要が証明されてから上に進むのが鉄則。

**最大の投資はデータ整備と評価設計** に向けるべきで、モデル選択は二の次である。CineBASIC で経験された「揺らぎ」は、データと評価の不足から来る本質的問題であり、より高度なモデルや訓練手法では解決しない。

エンジニアとして本構想に取り組むなら、まず **#1 で限界を見極め、#3 で知識を持たせ、必要なら #4 に進む** という段階的アプローチが現実的であり、論文化の観点でも最適である。

---

**改訂履歴**:
- v1（2026-05-13）: 初版作成
